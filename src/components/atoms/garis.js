import React from 'react';
import { View, TouchableOpacity,Text } from 'react-native';
import text from './text' 
import { css } from '../../assets/css/cssku';

const garis = (props) => {
    return (
     <View style={css.garis} />
    ); 
}

export default garis

